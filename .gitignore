# Local Claude Code working config — dev copy of the plugin (repo root is canonical)
.claude/

# Research corpus (third-party articles, not distributable)
docs/

# Internal planning notes
LOOP_SKILL_PLAN.md
tasks/
.superpowers/

# Convergence-round bookkeeping (round log, backlog, defect patterns).
# Ignored rather than tracked for the same reason .loop/ is: this repo's own
# loops and unattended drivers refuse to run on a dirty tree, so bookkeeping
# that lives beside the source must not show up as an untracked change.
.converge/

# Loop runtime state and logs
.loop/
*.log

# Tooling caches
.code-graph/
node_modules/
